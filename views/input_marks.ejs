<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Input Marks</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #667eea, #764ba2);
            color: #333; /* Changed text color for better readability */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            padding: 50px 0; /* Adjusted padding */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .card {
            background: rgba(255, 255, 255, 0.9); /* Increased opacity for better contrast */
            border: none;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            margin-bottom: 20px;
            width: 100%;
            max-width: 600px; /* Ensures the card does not get too wide */
        }

        .card h2 {
            font-weight: bold;
            color: #333; /* Changed text color for better readability */
        }

        .btn {
            background-color: #6c5ce7;
            border-radius: 20px;
            font-weight: bold;
            color: white;
            transition: 0.3s;
        }

        .btn:hover {
            background-color: #a29bfe;
            color: #fff;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .btn-back {
            background-color: #0056b3;
            border-color: white;
            color: white;
            transition: background-color 0.3s ease;
            margin-bottom: 20px; /* Added margin-bottom to separate it from the form */
            display: inline-block; /* Ensure it does not stretch */
        }

        .btn-back:hover {
            background-color: red;
            border-color: #545b62;
            color: #ffffff;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <h1 class="text-center mb-4">Input Marks</h1>
            
            <!-- Back to Dashboard Button -->
            <div class="text-center mb-4">
                <a href="/teacher-dashboard" class="btn btn-back">Back to Dashboard</a>
            </div>

            <form action="/submit-marks" method="POST"> <!-- Form tag added -->
                <% members.forEach(member => { %>
                    <div>
                        <h2>Student: <%= member.name %> (ID: <%= member.student_id %>)</h2>
                        <div class="form-group">
                            <label for="marks_<%= member.student_id %>">Marks:</label>
                            <input type="number" id="marks_<%= member.student_id %>" name="marks[<%= member.student_id %>]" class="form-control" required> <!-- Updated input name -->
                        </div>
                        <input type="hidden" name="student_id" value="<%= member.student_id %>">
                    </div>
                <% }) %>
                <div class="text-center">
                    <button type="submit" class="btn">Submit Marks</button> <!-- Added type attribute -->
                </div>
            </form> <!-- Form tag closed -->
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
