<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Meetings</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            background-color: #f8f9fa;
            color: #333;
            font-family: 'Roboto', sans-serif;
            padding-top: 20px;
        }

        .container {
            margin-top: 50px;
            padding: 0 20px;
        }

        .meeting-card {
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
            padding: 20px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .meeting-card:hover {
            transform: translateY(-5px);
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
        }

        .meeting-card h4 {
            margin-bottom: 10px;
            color: #007bff;
            font-weight: bold;
        }

        .meeting-card p {
            margin-bottom: 5px;
            font-size: 1.1rem;
        }

        .meeting-details {
            font-weight: bold;
            display: inline-block;
            min-width: 100px;
            margin-right: 10px;
            vertical-align: top;
        }

        .meeting-details-team {
            color: #333;
        }

        

        .btn-edit {
            color: #ffffff;
            background-color: #007bff;
            border: none;
            border-radius: 30px;
            padding: 10px 20px;
            text-decoration: none;
            transition: background-color 0.3s ease, transform 0.3s ease;
            display: inline-block;
        }

        .btn-edit:hover {
            background-color: #b0f8bc;
            transform: translateY(-3px);
        }

        .btn-dashboard {
            color: #ffffff;
            background-color: #6c757d;
            border: none;
            border-radius: 30px;
            padding: 10px 20px;
            text-decoration: none;
            transition: background-color 0.3s ease, transform 0.3s ease;
            display: inline-block;
            margin-bottom: 20px;
        }

        .btn-dashboard:hover {
            background-color: #ffffff;
            transform: translateY(-3px);
        }

        .navbar {
            background-color: #007bff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-weight: 500;
            font-size: 1.5rem;
            color: #ffffff;
        }

        .navbar-brand:hover {
            color: #e0eafc;
        }
    </style>
</head>

<body>
    <!-- Header -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <img src="icons/iut-logo.jpeg" width="40" height="50" class="d-inline-block align-top" alt="">
            <a class="navbar-brand" href="#">View Meetings</a>
        </div>
    </nav>

    <div class="container">
        <a href="/student-dashboard" class="btn-dashboard"><i class="fas fa-arrow-left"></i> Back to Dashboard</a>
        <h1 class="mb-4">View Meetings</h1>
        <% meetings.forEach(function(meeting) { %>
        <div class="meeting-card">
            <h4>Meeting ID: <%= meeting.id %></h4>
            <p>
                <span class="meeting-details meeting-details-team-id">Team ID:</span>
                <%= meeting.team_id %>
            </p>
            <p>
                <span class="meeting-details meeting-details-date">Date:</span>
                <%= new Date(meeting.meeting_date).toDateString() %>
            </p>
            <p>
                <span class="meeting-details meeting-details-time">Time:</span>
                <%= meeting.meeting_time %>
            </p>
            <p>
                <span class="meeting-details meeting-details-details">Details:</span>
                <%= meeting.meeting_details %>
            </p>
            <p>
                <span class="meeting-details meeting-details-notes">Notes:</span>
                <%= meeting.meeting_notes %>
            </p>
            <a href="/student-edit-meeting-notes/<%= meeting.id %>" class="btn-edit">Edit Notes <i class="fas fa-edit"></i></a>
        </div>
        <% }); %>
    </div>

    <!-- Bootstrap core JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
