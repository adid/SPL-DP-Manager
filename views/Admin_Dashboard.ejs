<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=yes" />
    <title>Admin Dashboard</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <!-- Custom CSS -->
    <style>
        body {
            color: #343a40;
            font-family: 'Roboto', sans-serif;
            background-image: linear-gradient(to right, white, #f0f0f0);
        }

        .navbar {
            background-color: #007bff;
            padding: 15px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
        }

        .navbar-brand {
            font-weight: bold;
            color: white;
            font-size: 24px;
        }

        .container {
            padding-top: 50px;
        }

        .dashboard-title {
            font-size: 36px;
            text-align: center;
            margin-bottom: 30px;
            color: #007bff;
            font-weight: bold;
        }

        .card {
            background: rgba(255, 255, 255, 0.9);
            border: none;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.2);
        }

        .card h2 {
            font-weight: bold;
            color: #343a40;
            text-align: center;
            margin-bottom: 20px;
        }

        .btn {
            background-color: #007bff;
            border-radius: 20px;
            font-weight: bold;
            color: white;
            transition: 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .btn i {
            margin-right: 8px;
        }

        .btn-light {
            background-color: #e0eafc;
            color: #007bff;
        }

        .btn-light:hover {
            background-color: #cfdef3;
            color: #0056b3;
        }

        .btn:hover {
            background-color: #0056b3;
            color: #fff;
        }

        .logout-btn {
            background: linear-gradient(45deg, #ff416c, #ff4b2b);
            border: none;
            margin-top: -20px;
            border-radius: 20px;
            padding: 10px 20px;
            color: white;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .logout-btn:hover {
            background: linear-gradient(45deg, #ff4b2b, #ff416c);
            transform: translateY(-3px);
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
        }

        .logout-btn:active {
            background: linear-gradient(45deg, #ff4b2b, #ff416c);
            transform: translateY(-1px);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
        }

        .logout-btn:focus {
            outline: none;
            box-shadow: 0 0 0 0.2rem rgba(255, 75, 43, 0.5);
        }

        .notification-btn {
            background-color: #e74c3c;
            border-radius: 20px;
            font-weight: bold;
            color: white;
            transition: 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .notification-btn.unread {
            background-color: #e74c3c;
        }

        .notification-btn.read {
            background-color: #6c5ce7;
        }

        .info-item {
            margin-bottom: 20px;
            display: flex;
            align-items: center;
        }

        .info-item i {
            color: #007bff;
            margin-right: 15px;
            font-size: 24px;
        }

        .info-item span {
            font-weight: bold;
            color: #007bff;
            font-size: 18px;
            font-family: 'Roboto', sans-serif;
        }

        .info-item p {
            display: inline-block;
            margin: 0;
            font-size: 18px;
            color: #343a40;
            margin-left: 10px;
            font-family: 'Cursive', sans-serif;
            font-weight: bold;
        }

        .info-card {
            text-align: left;
            padding: 20px;
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar">
        <div class="container">
            <a class="navbar-brand" href="#">Admin Dashboard</a>
        </div>
    </nav>

    <!-- Dashboard Section -->
    <div class="container">
        <h1 class="dashboard-title">Welcome to the Admin Dashboard</h1>

        <div class="row">
            <div class="col-md-4">
                <div class="card">
                    <h2>Actions</h2>
                    <a href="/students" class="btn btn-block mb-2"><i class="fas fa-users"></i> View Student List</a>
                    <a href="/teachers" class="btn btn-block mb-2"><i class="fas fa-chalkboard-teacher"></i> View Teacher List</a>
                    <a href="/supervisors" class="btn btn-block mb-2"><i class="fas fa-upload"></i> Upload Supervisor Load</a>
                    <a href="/teams" class="btn btn-block mb-2"><i class="fas fa-users-cog"></i> View Teams</a>
                    <a href="/send-notice" class="btn btn-block mb-2"><i class="fas fa-envelope"></i> Send Notice</a>
                    <a href="/admin/input-marks" class="btn btn-block mb-2"><i class="fas fa-pen"></i> Insert Marks</a>
                    <a href="/admin-view-marks" class="btn btn-block mb-2"><i class="fas fa-eye"></i> View Marks</a>
                </div>
            </div>
            <div class="col-md-8">
                <div class="card">
                    <h2>Admin Information</h2>
                    <p>ID: <%= admin.id %></p>
                    <p>Username: <%= admin.username %></p>
                    <p>Role: <%= admin.role %></p>
                    <a href="/admin-notifications" class="btn notification-btn <%= hasUnreadNotifications ? 'unread' : 'read' %>"><i class="fas fa-bell"></i> Notifications</a>
                </div>
            </div>
        </div>

        <div class="text-center mt-5">
            <a class="btn logout-btn" href="/user-logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>
    </div>

    <!-- Bootstrap core JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
