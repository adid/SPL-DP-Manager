<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Meetings</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            background-color: #f8f9fa; /* Set background color */
            color: #333; /* Set text color to dark gray */
            font-family: Arial, sans-serif; /* Use a sans-serif font */
            padding-top: 20px; /* Add padding to the top */
        }

        .container {
            margin-top: 50px;
            padding: 0 20px; /* Add padding to container */
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #d5d5d5; /* Add bottom border to table cells */
        }

        th {
            background-color: #007bff; /* Set header background color */
            color: #000000; /* Set header text color to white */
        }

        tr:nth-child(even) {
            background-color: #f2f2f2; /* Alternate row background color */
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="mb-4">View Meetings</h1>
        <table class="table table-bordered table-striped">
            <thead class="thead-dark">
                <tr>
                    <th>ID</th>
                    <th>Team ID</th> <!-- New column for Team ID -->
                    <th>Date</th>
                    <th>Time</th>
                    <th>Details</th>
                </tr>
            </thead>
            <tbody>
                <% meetings.forEach(function(meeting) { %>
                    <tr>
                        <td><%= meeting.id %></td>
                        <td><%= meeting.team_id %></td> <!-- Display Team ID -->
                        <td><%= new Date(meeting.meeting_date).toDateString() %></td>
                        <td><%= meeting.meeting_time %></td>
                        <td><%= meeting.meeting_details %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
    <!-- Bootstrap core JS-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>